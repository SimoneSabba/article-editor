'use strict';

var myApp = angular.module('app', ['ui.bootstrap']);

myApp.controller(
	'AppCtrl', ['$scope', 'productsService', 'blockService', 
		function($scope, productsService, blockService) {
			
	}]);
